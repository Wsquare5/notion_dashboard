# Copy to .env and fill values
# Obtain a Notion integration token and create a database (see README for steps)
NOTION_TOKEN=secret_xxx
NOTION_DATABASE_ID=your_database_id_here
# Optional mapping overrides: JSON file path mapping base asset -> coingecko id
MAPPING_OVERRIDES_FILE=./overrides.json
